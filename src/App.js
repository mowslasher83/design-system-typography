import React from 'react';
import { Typography } from './index';
import './style.css';

const App = () => {
  return (
    <div>
      <Typography variant="h1">Chào mừng đến với hệ thống thiết kế kiểu dáng chữ!</Typography>
      <Typography variant="body">Đây là một ví dụ đơn giản để bắt đầu.</Typography>
    </div>
  );
};

export default App;
